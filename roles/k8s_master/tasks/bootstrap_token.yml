---
- name: create bootstrap token from j2
  template:
    src: token.csv.j2
    dest: /etc/kubernetes/token.csv
    owner: root
    group: root
    mode: 0755
  tags:
    - k8s
    - k8s_install
    - k8s_bootstrap_token
